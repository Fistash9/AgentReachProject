# Agent Reach — Журнал видалень

> Кожне видалення файлів чи директорій, зроблене агентом (Claude Code),
> записується сюди ПЕРЕД виконанням, не після. Ніщо не видаляється
> назавжди без запису тут — правило додано 2026-09-19 після реального
> прецеденту "trash guard" для AI-агентів (задокументовано випадок,
> коли інший агент випадково видалив цілу домашню директорію).
>
> Для нетривіальних видалень (не очевидний сміттєвий артефакт) —
> спершу перемістити в `.trash/` (локально, не в git), а не `rm -rf`
> напряму. Для очевидних, щойно самим створених артефактів (як
> нижче) — прямий запис у лог достатній, `.trash/` не обов'язковий.

## Записи

### 2026-09-19 — node_modules, package.json, package-lock.json
- **Що:** випадково створені артефакти (`npm install slopsquash` без
  `-g` в робочій директорії проєкту)
- **Чому видалено:** нічого в проєкті на них не посилається,
  0 вразливостей, не зареєстровано як MCP-сервер — підтверджено
  трьома окремими перевірками перед видаленням (див. TROUBLES.md,
  "Тестування hook через реальну дію замість симуляції")
- **Спосіб:** пряме `rm -rf` (не `.trash/`) — самі щойно створені
  цією ж сесією артефакти, не існуючі дані проєкту
- **Відновлюване:** так, тривіально — `npm install slopsquash`
  відтворить те саме за секунди, якщо колись знадобиться

### 2026-09-23 — hook-block-test.txt
- **Що:** одноразовий тестовий файл, створений цією ж сесією для
  живої перевірки нового хука trash-md-guard-hook.py (rm/rmdir →
  TRASH.md, PreToolUse, блокує)
- **Чому видалено:** мета створення — довести, що хук реально
  блокує `rm` без свіжого запису тут; файл не містить нічого, крім
  тестового рядка
- **Спосіб:** пряме `rm` — щойно самим створений артефакт цієї сесії
- **Відновлюване:** так, тривіально — сам факт видалення й є доказом
  роботи хука

### 2026-09-23 — .claude/logs/delegate-calls.jsonl (тестові записи)
- **Що:** 3 тестові рядки логу (успіх/помилка/фон), створені pipe-
  тестом нового delegate-outcome-logger-hook.py
- **Чому видалено:** синтетичний тестовий вміст, не реальні дані;
  файл продовжить наповнюватись реальними записами після живого
  використання
- **Спосіб:** пряме `rm` — щойно самим створений артефакт цієї сесії
- **Відновлюване:** так — файл просто почне заповнюватись заново

### 2026-09-23 — .claude/logs/delegate-calls.jsonl (другий тестовий запис)
- **Що:** 1 тестовий рядок, доданий pipe-тестом виправленого логера
  (після додавання PostToolUseFailure — перевірка, що поле "error"
  теж читається)
- **Чому видалено:** знову синтетика, не реальні дані
- **Спосіб:** пряме `rm` — щойно самим створений артефакт цієї сесії
- **Відновлюване:** так

### 2026-09-24 — tools/verifier-backtest/sources/ (6 чужих файлів — лише з git)
- **Що:** anthropic-multi-agent.txt, arxiv-2512.08296-abs.txt,
  arxiv-2512.08296.txt, baton-server.mjs.txt, baton-PROTOCOL.md,
  freellmapi-README.md
- **Чому:** репозиторій публічний. Це копії чужих текстів (стаття
  Anthropic, arXiv; MIT-файли без тексту ліцензії). Рішення
  користувача: «Прибрати з git».
- **Спосіб:** `git filter-branch --index-filter 'git rm --cached …'` на
  9 незапушених комітах (origin/main..main). Файли ЛИШАЮТЬСЯ на диску,
  внесено в .gitignore; як скачати заново — sources/README.md.
- **Відновлюване:** так — гілка `backup/before-sources-filter-2026-09-24`
- **Інцидент (дописано одразу після):** припущення «файли лишаться на
  диску» було хибним. `filter-branch` наприкінці оновив робоче дерево,
  і 6 файлів зникли з диска. Відновлено `git show backup/…:<шлях> >
  <шлях>`, розміри збіглися, копія baton — байт у байт (`cmp`).

### 2026-09-26 — claude-deepseek.sh.bak-20260926
- Що: власний щойно створений `.bak` перед заміною моделей у
  claude-deepseek.sh (pro/v4-flash → deepseek-flash, рішення користувача).
- Чому видалено: файл під git — за правилом «Бекап поза git» git і є
  бекап; `.bak` не в .gitignore і міг би потрапити в коміт. Очевидний
  артефакт власної помилки — без `.trash/`.

### 2026-09-26 — .baton/history/pass-01…pass-69.json, _vanished-items.json → .trash/
- Що: 68 знімків baton, відновлених того ж дня з журналів сесій
  (pass-01…pass-68 без #6, pass-69), і _vanished-items.json (233 зниклі
  пункти). pass-70.json лишається — до baton_pick_up нової сесії.
- Чому: аудит зроблено, корисний зміст — у trees/baton-audit-2026-09-26.md
  (усі 233 пункти + таблиці); рішення користувача «зайві батони видалити
  в кінці», спосіб — .trash/ (вибір користувача).
- Відновлюване: так — `.trash/baton-history-2026-09-26/` (поза git);
  повернути: `mv .trash/baton-history-2026-09-26/* .baton/history/`.
