# Agent Reach — Журнал видалень

> Кожне видалення файлів чи директорій, зроблене агентом (Claude Code),
> записується сюди ПЕРЕД виконанням, не після. Ніщо не видаляється
> назавжди без запису тут — правило додано 2026-09-19 після реального
> прецеденту "trash guard" для AI-агентів (задокументовано випадок,
> коли інший агент випадково видалив цілу домашню директорію).
>
> Для нетривіальних видалень (не очевидний сміттєвий артефакт) —
> спершу перемістити в `.trash/` (локально, не в git), а не `rm -rf`
> напряму. Для очевидних, щойно самим створених артефактів (як
> нижче) — прямий запис у лог достатній, `.trash/` не обов'язковий.

## Записи

### 2026-09-19 — node_modules, package.json, package-lock.json
- **Що:** випадково створені артефакти (`npm install slopsquash` без
  `-g` в робочій директорії проєкту)
- **Чому видалено:** нічого в проєкті на них не посилається,
  0 вразливостей, не зареєстровано як MCP-сервер — підтверджено
  трьома окремими перевірками перед видаленням (див. TROUBLES.md,
  "Тестування hook через реальну дію замість симуляції")
- **Спосіб:** пряме `rm -rf` (не `.trash/`) — самі щойно створені
  цією ж сесією артефакти, не існуючі дані проєкту
- **Відновлюване:** так, тривіально — `npm install slopsquash`
  відтворить те саме за секунди, якщо колись знадобиться

### 2026-09-23 — hook-block-test.txt
- **Що:** одноразовий тестовий файл, створений цією ж сесією для
  живої перевірки нового хука trash-md-guard-hook.py (rm/rmdir →
  TRASH.md, PreToolUse, блокує)
- **Чому видалено:** мета створення — довести, що хук реально
  блокує `rm` без свіжого запису тут; файл не містить нічого, крім
  тестового рядка
- **Спосіб:** пряме `rm` — щойно самим створений артефакт цієї сесії
- **Відновлюване:** так, тривіально — сам факт видалення й є доказом
  роботи хука
