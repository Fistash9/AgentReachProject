# Agent Reach — Відкладені задачі

> Список того, що відкладено «на потім». Перевіряти при переході
> до нової теми та наприкінці сесії. Оновлювати статуси.

## Активні (плануємо зробити)

- **Instagram Reels — приватні акаунти не перевірено.** Для ПУБЛІЧНИХ
  Reels transcriptor MCP уже працює без cookies (див. Завершено).
  Приватні акаунти/reels — cookies (Kiwi Browser + yt-dlp --cookies)
  можуть знадобитись саме для них, окремо не тестувалось.
- **Нагадування: на початку КОЖНОЇ сесії викликати `baton_pick_up`
  (2026-09-20).** У журналі baton двічі поспіль сесія стартувала без
  pick_up (запис pass від 2026-09-20 06:39 прямо про це і наступна
  сесія після pass 11:23). Через це `session-close` бере за точку
  відліку pick_up попередньої сесії, і список комітів кроку 2 включає
  чужі коміти. Правило вже є в RULES.md ("На початку сесії — викликати
  baton_pick_up"); це нагадування, а не нове правило
- **Встановити grill-me і claude-setup (запит користувача, 2026-09-23).**
  - grill-me — назва зі слів користувача; що це і чий — ще НЕ шукали
    [unverified].
    - Пошук зроблено (2026-09-23, deepseek + звірка GitHub API/raw):
      це СКІЛ, що допитує користувача про ідею/план до чітких рішень.
      Оригінал — `mattpocock/skills` → `skills/productivity/grill-me/`
      (MIT, оновл. 2026-09-18): лише за `/grill-me`
      (disable-model-invocation), делегує в сусідній скіл `grilling` —
      ставити обидва; файлів не пише. Спосіб установки ще не дивились.
      Альтернатива — `RobMitt/grill-me-skill` (676 ⭐, без ліцензії,
      оновл. 2026-04-11): авто-тригер на "grill me", копіювати в
      `~/.claude/skills/grill-me/`. Копії TimothyVang/paveg — не
      перевірені. DeepSeek помилково позначив зв'язок із Matt Pocock
      як [unverified].
    - Статус змінився (2026-09-23): спосіб установки подивились (README
      mattpocock/skills). Лише весь набір: плагін
      `/plugin install mattpocock-skills` (є в офіційному маркетплейсі
      anthropics/claude-plugins-official, 25 скілів, хуків немає) або
      `npx skills@latest add mattpocock/skills` (vercel-labs `skills`,
      можна вибрати окремі скіли). Після — `/setup-matt-pocock-skills`.
      4 автоматичні скіли конфліктують з нашими файлами — ECOSYSTEM.md
      "Порівняння: скіли Matt Pocock vs наша система". Рішення: брати
      вибірково; ще не встановлено.
  - claude-setup — під цією назвою кілька сторонніх проєктів (жоден не
    Anthropic): плагін `HOPLAtools/claude-setup` (через `/plugin
    marketplace add`, вміст ще не читали) і npm `claude-setup` v2.0.3
    (AbdoKnbGit) — генерує/перезаписує CLAUDE.md, .mcp.json, хуки,
    settings.json. Перед установкою: уточнити, який саме, прочитати код,
    перевірити, що не перетре наші 6 хуків і правила.

- **Перевірити вартість deepseek-v4-pro vs v4-flash (запит користувача,
  2026-09-23).** Користувач вважає pro дорогим — не перевірено. Для
  порівняння скілів Matt Pocock 5 викликів зроблено на flash (параметр
  `model` у виклику); дефолт deepseek-mcp (pro) НЕ змінювався — чи
  змінювати, вирішити після перевірки цін і якості.

- **E5-хук: «неможливо/не існує» без пошуку (2026-09-23).** Тест на
  історії (TROUBLES.md "claimcheck: тест на історії") — E5 точний (2/2,
  0 хибних), але налаштований на тих самих даних і лише 1 випадок у
  відкладеній вибірці. Перед будівництвом: знайти ще 2-3 старі сесії з
  розміченими "неможливо без пошуку", прогнати
  `tools/claimcheck/backtest.py` без змін шаблонів; якщо 0 хибних —
  вузький Stop-хук (additionalContext, пропуск під-сесій deepseek за
  моделлю в журналі).

## Дослідити (можливо, колись)

### Нездійснені знахідки аудиту скілів (2026-09-20)
- **Походження:** аудит `.claude/skills` через два виклики `delegate`
  (DeepSeek). Застосовано лише A1, A12, C1 (b627629). Вибіркова
  перевірка знахідок — у TROUBLES.md. Повний вивід DeepSeek у файл не
  збережено, нижче — стислий перелік
- **request-brief / session-close, не застосовано:** A2 (RULES.md і
  CLAUDE.md названо двома різними файлами, а це один), A3 (назва
  розділу "DeepSeek" не збігається із заголовками в CLAUDE.md), A6/A7
  (скіл не згадує тригер автоделегування, а "чекай підтвердження брифу"
  розходиться з "без y/n"), A11 (`baton_history` не згаданий у розділі
  Baton в CLAUDE.md), B1–B9 (перекази правил CLAUDE.md в скілах — лишити
  покажчики), C2 (незакомічені правки CONTEXT.md невидимі для скрипта),
  C3–C5 (межа сесії за датою коміта, скрипт не прив'язаний до одного
  репо, кілька кореневих комітів)
- **Статус перевірки:** A9 і A10 спростовано (див. TROUBLES.md); A4, A5,
  C2, C3, C5 самим не перевірялись [unverified]
- **unlazy:** знахідки B1–B20 (дублювання в документації) навмисно не
  правляться — сторонній скіл, правки розійдуться з upstream
- Статус: нічого з переліку не робилось

### Розбіжність хуків із правилом автоделегування (2026-09-20)
- **Що:** RULES.md (розділ "DeepSeek — автоматичне делегування за
  тригером") велить делегувати без питання y/n, але PreToolUse-хуки на
  читання великих файлів і на Skill досі додають текст "run the
  delegation gate ... Delegate to DeepSeek? (y/n)" (спостережено в цій
  сесії кілька разів)
- **Наслідок:** протилежна інструкція в контексті без правила
  пріоритету. У цій сесії я слідував RULES.md (2 виклики `delegate` без
  y/n)
- **Не перевірено:** де налаштовані ці хуки і чи вони зі складу
  claude-code-deepseek-delegator — джерело нагадувань не шукалось
- **Варіанти (не обрано):** змінити текст хуків під правило проєкту або
  змінити правило. Рішення за користувачем
- Статус: не досліджувалось

### everything-claude-code (ECC) — набір скілів/агентів/хуків (2026-09-20)
- **Походження:** Instagram Reel `dashi_agent` (2026-09-19) — рекламний
  ролик, веде на збір контактів ("напиши *выходные*"), без посилання й
  технічних деталей. Транскрипція через transcriptor (Whisper)
- **Проєкт:** github.com/affaan-m/everything-claude-code (npm
  `ecc-universal`; за метаданими npm репозиторій перейменовано на
  affaan-m/ECC). MIT, Node >=18. Існує, не вигадка
- **Перевірено файлами (поверхневий клон, лише читання, нічого не
  запускалось):** 68 агентів, 292 скіли (кожен з SKILL.md), 94 команди;
  `hooks.json` — 7 типів подій і 24 команди, усі 24 скрипти існують; у
  `scripts/hooks` і `scripts/lib` немає `eval`/`new Function`; мережеві
  виклики — локальний 127.0.0.1, власний `/api/snapshot`, опційний
  реєстр registry.nasiko.dev, CDN для mermaid, окремий Discord-бот
- **Виправлення власних помилок цієї сесії:** казав "15+ хук-подій" зі
  слів переказу README — насправді 7 типів/24 команди; казав "Termux не
  згадується" — є розділ про OpenCode на Termux/Android у
  TROUBLESHOOTING.md (не про Claude Code)
- **Ролик неточний:** "286 скілів" застаріло (зараз 292, у README лінія
  2.0 = 281); "працює в Cursor/Codex/Gemini" перебільшено — Codex має
  sync, решта лише обмежені адаптери
- **[unverified]:** "2798 комітів" і "10 місяців розробки" (клон
  поверхневий); README-фраза "never uploads diagnostics" (у коді не
  підтверджено, 157 місць з `child_process` не читались); значення
  `ECC_*` за замовчуванням (8000 / 0.7 / 6); AgentShield (у коді не
  шукали); вміст 292 скілів і 68 агентів не читався
- **Ризики (не вигадки):** інсталятор переписує `~/.claude/settings.json`
  без бекапу (не знайдено в коді інсталятора; пошук за патерном, не
  повний аудит) — суперечить правилу `.bak` перед зміною; у `bin` є
  `ecc-memory-mcp` (RULES.md забороняє memory MCP для щоденної роботи);
  `compute-sponsor.js` виводить рекламу спонсора Itô (розкрито в коді);
  можливе конфліктування 24 хуків з нашими трьома
- **Що потенційно корисно:** перевірка секретів (sk-/ghp_/AKIA) перед
  виконанням — у нас є правило "не комітити agent.py", але немає
  автоматичної перевірки; AgentShield як заміна ручних аудитів skills;
  ідея ліміту контексту на старті (`ECC_SESSION_START_MAX_CHARS`)
- **Не робити:** ставити цілком; писати "выходные" (збір контактів)
- Аналіз статичний (шаблонний пошук), не повний аудит безпеки. Клон
  лежав у scratchpad сесії (тимчасовий, може зникнути)
- Статус: не встановлено; дослідити окремі ідеї, а не весь пакет

### tscribe — CLI-транскрипція X/YouTube (2026-09-20, [unverified])
- **Походження:** слово "tscribe" повторювалось 10 разів у полі
  "next" `baton_pass` з 2026-09-18, ніколи не визначене й не
  досліджене (див. TROUBLES.md "Знахідка: tscribe")
- **Знайдений кандидат:** github.com/rvben/tscribe — Rust CLI,
  транскрибує відео/аудіо URL (YouTube, **X/Twitter**, підкасти) у
  agent-friendly markdown через whisper.cpp (локально, без API-ключів,
  через yt-dlp + ffmpeg). README прочитано напряму (не з переказу
  пошуку). MIT, є CI, опубліковано на crates.io — реальний, живий
  проєкт
- **Чому саме цей кандидат:** прямо закриває відкритий пункт
  "transcriptor TikTok/X test" (transcriptor MCP орієнтований на
  YouTube; tscribe явно вміє X з коробки)
- **[unverified]:** чи саме ЦЕЙ репозиторій мався на увазі в
  оригінальному повідомленні — прямого посилання чи опису там не
  було, це найточніший реально існуючий відповідник за назвою й
  темою, не підтверджений факт
- Статус: дослідити встановлення/сумісність з Termux, якщо буде
  рішення спробувати

### A2A-апгрейд telegram_deepseek_bot.py (2026-09-19)
- **Мета:** замінити крихкий regex-парсинг рядка `CLAUDE_CODE: <промпт>`
  на реальний протокол делегування задач між DeepSeek-оркестратором
  і Claude Code
- **Перевірено:** офіційний SDK `pip install a2a-sdk`, hello-world
  реалізація — приблизно 1 день роботи, 150+ компаній підтримали
  протокол за рік (2026-09-19)
- **Не перевірено (реальні невідомі, щоб не здивуватись пізніше):**
  чи є готовий A2A Agent Executor wrapper для CLI-агента типу
  `claude -p`, чи доведеться писати самим; A2A вимагає постійного
  HTTP-сервера (JSON-RPC 2.0 + SSE) — інша архітектура, ніж поточний
  простий polling-цикл бота
- **Важливе обмеження, не плутати:** A2A покращує СТРУКТУРУ
  делегування (Agent Card, task lifecycle), але НЕ вирішує ризик
  №2 з ECOSYSTEM.md (немає перевірки, чи інструкція DeepSeek
  коректна) — це різні задачі
- **Питання пропорційності:** чи виправданий повний HTTP A2A-сервер
  для Telegram-бота одного користувача, чи це той самий
  over-engineering, від якого відмовились сьогодні для інших ідей
- Статус: обговорити деталі перед рішенням про реалізацію

### claude-mem / claude-code-auto-memory — автопам'ять (2026-09-19)
- **claude-mem** (github.com/thedotmack/claude-mem) — 94.2k зірок
  (перевірено двічі незалежно: GitHub API + пряме читання сторінки).
  Захоплює все, що агент робить під час сесії, стискає через AI,
  сам вприскує релевантний контекст у майбутні сесії. Працює з
  Claude Code, Codex, Gemini та іншими
- **claude-code-auto-memory** (github.com/severity1/claude-code-auto-memory,
  158 зірок) — плагін, у реальному часі відстежує edit/delete/move
  і тихо оновлює CLAUDE.md у фоні, ізольований агент (не займає
  контекст головної розмови)
- **Чому цікаво:** обидва автоматичні (не треба самому пам'ятати
  покликати перевірку) — на відміну від ручного zvirka-bazy
- Статус: дослідити, не встановлено

### Jules (Google) — асинхронний coding-агент (2026-09-18)
- **Що:** клонує GitHub-репозиторій у хмарну VM, пише план, вносить
  зміни, запускає тести, відкриває Pull Request — асинхронно, поки
  користувач робить щось інше. Працює на Gemini-моделях
- **Перевірено:** реальний, безкоштовний тариф — 15 задач/день,
  3 паралельно. CLI: `npm install -g @google/jules` (офіційний
  пакет, підтверджено), потребує Google-акаунт
- **Чому цікаво:** асинхронна модель частково закриває те саме, що
  й ідея Telegram-бота (робота йде, поки тебе немає) — тільки через
  GitHub PR, без потреби писати власну інфраструктуру
- **Застереження:** ще один окремий акаунт/канал, куди йде код
  репозиторію (хмарна VM) — не інтегрувати одразу в постійний
  воркфлоу, спочатку спробувати на одній задачі
- **Рекомендація:** спробувати на одній конкретній задачі з BACKLOG
  (наприклад, transcriptor — тест TikTok/X) як разовий експеримент
- Статус: дослідити, не встановлено

### Автоматизація git (PostToolUse/Stop хуки) (2026-09-18)
- PostToolUse: `git add -A && git commit -m 'claude: auto-save'`
- Stop-хук: git-auto-commit-on-stop (сканує секрети, підтримує
  `SKIP_AUTO_COMMIT`)
- ⚠️ Застереження: Stop-хук спрацьовує після КОЖНОГО ходу — небезпечно
  без guard
- Пакети (перевірено 2026-09-18): tapout (PyPI, 0.1.2), task-journal-mcp
  (crates.io, 0.28.4), context-auto-handoff (npm, 1.7.1)
- Статус: розглянути (не встановлювати без guard)

### baton_pass — автоматизація через SessionEnd (2026-09-18)
- tapout: SessionEnd hook генерує підсумок замість ручного baton_pass
- ⚠️ Може перезаписати HANDOFF.md — потрібен append-режим або guard
  за mtime
- **Частково закрито (2026-09-19):** `baton-reminder-hook.py` —
  PreToolUse на `git push`, рахує коміти від останнього pass,
  нагадує (не генерує сам handoffNote — це все ще вручну). Перевірено
  наживо, спрацював коректно
- Статус: залишається — повна автогенерація підсумку (як tapout)
  все ще не зроблена, тільки нагадування про потребу

### Урок про gate — занадто агресивний (2026-09-18)
- Hook питає "Delegate?" на кожне дрібне правило, майже завжди
  відповідь "ні"
- Можливо, поріг варто підняти (не 300 рядків, а більше)
- **Знайдено (2026-09-18):** `claude-model-router-hook` (github.com/
  tzachbon/claude-model-router-hook, 83 зірки, реальний, оновлений
  2026-09-15) — класифікує складність задачі через keyword/pattern
  matching (zero API calls), а не тільки довжину промпту
- **Принцип (LiteLLM AutoRouter, підтверджено дослідженнями):** довжина
  промпту сама по собі — погана евристика ("короткий промпт може бути
  дуже складним, а довгий — тривіальним"); краще комбінувати кілька
  сигналів (наявність коду, технічні терміни, reasoning-мова)
- Статус: калібрування classify-task.sh — розглянути перехід на
  multi-signal підхід замість чистої довжини повідомлення

### claude-archive-kb — архів розмов з Claude.ai app (2026-09-18)
- **Мета:** зібрати, перевірити і структуровано зберегти напрацьоване
  в застосунку Claude (комп'ютер + телефон) — нічого не видаляти
- **Крок 1 (дія користувача):** claude.ai → Settings → Privacy →
  Export data. Anthropic шле на email ZIP з `conversations.json`.
  Посилання діє 24 год. Працює тільки з web/desktop — **на iOS/Android
  експорту немає** (мобільний і десктопний клієнт — той самий акаунт,
  тож експортувати можна через комп'ютер/веб)
- **Обмеження:** persistent memory Claude в експорт НЕ входить,
  тільки самі розмови
- **Крок 2 (аналіз):** конвертувати `conversations.json` в окремі
  `.md`-файли (по розмові/темі), індексувати через SQLite FTS5 для
  швидкого повнотекстового пошуку без повторного вичитування файлів
- **Інструменти для пошуку:** ripgrep (`rg`) замість grep — стрімить
  файл, не завантажує весь у пам'ять/контекст LLM; `jq` для
  структурного пошуку по JSON. Обидва **не встановлені** в Termux
  (перевірено 2026-09-18, `command -v rg jq` — не знайдено)
- **Готові інструменти для аналізу (перевірено на GitHub, 2026-09-18):**
  conversation-analysis-tools (CrazyDubya), claude_desktop_export_browser
  (davidteren), claude-chat-viewer (osteele), Claude-Conversation-Exporter
  (socketteer, Chrome-розширення)
- **На що звертати увагу:** секрети/ключі у тексті розмов, дублікати
  тем на різних пристроях, застаріла/суперечлива інформація, розмір
  архіву (сотні-тисячі розмов — пряме читання все в LLM неможливе)
- **Статус:** дослідити — чекає на Крок 1 (export data) від користувача

### transcriptor — тест TikTok/X (2026-09-18)
- YouTube працює, Instagram публічні працюють
- TikTok/X не перевірено

### chrome-bridge-mcp — доступ Claude Code до Chrome (2026-09-18)
- **Мета:** дати Claude Code (Termux) доступ до залогіненого Chrome
  на комп'ютері для читання claude.ai чатів і налаштувань
- **Чому не офіційне Claude in Chrome:** розширення завжди підключається
  до десктопного Claude.app, ігнорує Claude Code; не призначене для
  експорту історії
- **Рішення:** chrome-bridge-mcp (github.com/frsorrentino/chrome-bridge)
- **Умови:** Node.js 18+, Chrome 135+
- **План:**
  1. Встановити на КОМП'ЮТЕРІ (не в Termux — ARM64 ризик)
  2. Увімкнути розширення в chrome://extensions (Developer mode)
- **Оновлення 2026-09-22:** новий тригер — доступ до платного курсу
  Media Bazar (kwiga.com), сторінка за логіном, потрібно прочитати
  5 уроків модуля «Створення відео з AI». Перевірено того ж дня:
  Claude in Chrome офіційно не підтримує мобільні пристрої (джерело:
  support.claude.com/en/articles/12902405); Kiwi Browser технічно вміє
  ставити .crx на Android, але не перевірено, чи парується з Claude
  Code; репозиторій chrome-bridge (frsorrentino/chrome-bridge) існує,
  214 комітів, підтверджено WebFetch. Користувач: комп'ютер є вдома,
  зараз лише телефон. Статус лишається "відкладено" — налаштувати,
  коли є доступ до комп'ютера в тій самій мережі.
  3. Підключити до Claude Code в Termux через HTTP + IP комп'ютера
- **Статус:** відкладено — зробимо пізніше

### claude-code-llm-router (llm-routing) — СКАСОВАНО (2026-09-17)
- **Проблема:** вимагає `mcp>=2.0.0`, що зламає agent-reach (жорстко пінований на `mcp<2.0.0`)
- **Обхідний шлях:** `pipx install` (ізольований venv, не чіпає глобальний mcp) — окрема задача
- **Статус:** скасовано (конфлікт залежностей)

### Міграція my_mcp_server.py на MCP SDK 2.x
Міграція my_mcp_server.py на MCP SDK 2.x — 20-30 рядків, 15-20 хв.
Робити тільки коли з'явиться конкретний інструмент, що вимагає mcp 2.x.
Before/after приклад знайдено, міграція готова під ключ.
Деталі — TROUBLES.md, розділ «MCP SDK 2.x — дослідження (2026-09-17)».

### DeepSeek CLI у терміналі
- **Мета:** окремий DeepSeek-чат із доступом до файлової системи
- **Варіанти:** deepseek-termux, dsh-termux, deepseek-agent-2026
- **Застереження:** DSH у Termux падає через glibc-runner
- **Статус:** відкладено

### A2A-шар для делегування між агентами
- **Мета:** «делегуючий агент» розподіляє задачі між іншими агентами
- **Що потрібно:** Agent Card, discovery, протокол A2A (Google)
- **Деталі:** ECOSYSTEM.md, розділ «MCP і A2A»
- **Статус:** концепція

### skills і agents від modelcontextprotocol
- **Мета:** Agent Skills + локальні субагенти
- **Що потрібно:** дослідити, чи сумісні зі stdio Claude Code (як memory — несумісний)
- **Статус:** не перевірено

### MCP-маніфест проєкту
- **Мета:** server.json з версією, інструментами, залежностями
- **Деталі:** ECOSYSTEM.md, розділ «Чекліст сумісності»
- **Статус:** концепція

### Додаткові інструменти в my_mcp_server.py
- **Ідеї:** search, save, list_feeds
- **Статус:** не визначено

### Freebuff (2026-09-18)
- **Що:** github.com/CodebuffAI/freebuff
- **Статус:** чекати PR #1377 (ARM Linux fix). Або @nyxur/freebuff
  (Termux-форк).

### Агент-аналізатор відео: огляд готових інструментів (2026-09-21)
- **Мета:** знайти готове для аналізу відео конкурентів так, щоб
  відповісти на 11 запитань агента-сценариста (тематика, мова,
  аудиторія, автор, хронометраж, research, точність, джерела, тон,
  особливі правила, формат), і вирішити: брати готове, збирати по
  частинах чи писати власне
- **Метод:** 3 вузькі виклики `deepseek` (пошук у вебі). Мною перевірено:
  що репозиторії існують, ліцензії, зірки, дати (GitHub API), ключові
  фрази документації Gemini, README BirdNET і README youtube-agent-skill.
  Можливості інструментів — зі слів README/документації, самі
  інструменти НЕ запускались
- **Головний висновок:** готового моноліту немає. Аудиторію, потребу в
  research, вимоги до точності, джерела й особливі правила не закриває
  жоден інструмент — це рішення користувача або висновок LLM поверх
  транскрипту. Інструменти діляться на шари: (1) текст сценарію
  (структура, хук, утримання), (2) метрики каналів і аутлаєри, (3) кадри
  й звук
- **Шар 1, текст (скіли для Claude):** Jakeschincariol/youtube-agent-skill
  (MIT, 11 скілів, шість Python-інструментів "no dependencies", ідея
  `voice.md` схожа на наш CHANNEL PROFILE; бере ВЛАСНІ транскрипти й
  експорт утримання, а не URL чужих відео; `hookscore.py` сам автор
  називає евристикою); shipshitdev/skills `youtube-video-analyst` (є
  розбір хуку, структури, утримання; ліцензії в GitHub API немає — тексти
  не копіювати, брати лише ідею); JuneYaooo/viral-structure-teardown
  (Apache-2.0, увесь вміст китайською, під короткі відео)
- **Шар 2, метрики:** YouTube Data API v3 (безкоштовно, публічні дані
  будь-якого відео: тривалість, теги, мова, канал; Analytics API чужих
  каналів не віддає); ericosiu/ai-marketing-skills (MIT, аутлаєри,
  потрібен ключ API); bangermap-mcp (npm, дуже новий); комерційні
  vidIQ, TubeBuddy, Social Blade, ViewStats, 1of10, HypeAuditor,
  NoxInfluencer — за їхніми сторінками лише метрики, текст сценарію не
  аналізують
- **Шар 3, кадри й звук:** Gemini API (за документацією приймає
  публічні YouTube-посилання, безкоштовно до 8 год YouTube на день,
  кадри 1 к/с, звук 1 Кбіт/с моно — птахів за таким звуком навряд чи
  розрізнить, це мій висновок); BirdNET-Analyzer (код MIT, але МОДЕЛІ
  CC BY-NC-SA: некомерційно, для монетизованих відео проблема);
  YAMNet (Apache-2.0) і PANNs (MIT) — розрізняють птахів, музику й
  мовлення, працездатність на Termux не перевірена; PySceneDetect
  (BSD-3, склейки кадрів)
- **НЕ ПЕРЕВІРЕНО:** vidiq.com/mcp (відповів 429), ціни сервісів,
  Spotter Studio (сторінки недоступні), YAMNet/TFLite на Termux, ціни
  Twelve Labs. Екосистема молода: більшість репозиторіїв 2026 року,
  0–400 зірок, тож якість промптів треба перевіряти вручну
- **Ключі:** для YouTube Data API і Gemini потрібні ключі, які я сам
  отримати не можу (створюються в акаунті Google користувача)
- **Пропозиція (не затверджена):** власний скіл-аналізатор у стилі
  `AGENT.md`: транскрипт (наш transcriptor) → розбір за чеклістом,
  ідея якого з шару 1 → вивід відповідей на 11 запитань і чернетка
  STYLE PROFILE; метрики додати Data API v3, кадри й звук — опційно
  потім
- **Питання "чи замінити Gemini на NVIDIA" (2026-09-21):** користувач
  приніс відповідь DeepSeek; перевірено мною за сторінками NVIDIA й
  Hugging Face. Підтверджено: Nemotron 3 Nano Omni існує, відкриті ваги,
  відео + звук + текст, доступний через build.nvidia.com, Hugging Face,
  OpenRouter; безкоштовний рівень NIM ~40 запитів/хв (вторинні джерела +
  форум NVIDIA); на прикладі звуку модель назвала "bird chirping" (рівень
  "птах співає", не вид). Не підтверджено або розходиться: "перевершує
  Gemini 2.5 Flash на DailyOmni/VoiceBench" (сторінки NVIDIA порівнюють
  лише з відкритими моделями, зокрема Qwen3-Omni); "9.91 год відео за
  годину" (не знайдено); назва ліцензії в DeepSeek "NVIDIA Open Model
  Agreement", а NVIDIA пише "NVIDIA Nemotron Open Model License", умови
  комерційного використання на її сторінках прямо не сформульовані.
  Ліміт довжини відео: пошук дав "відео до 2 хв, аудіо до 1 год"
  [unverified], HF-блог: аудіо навчене до 20 хв, контекст 5+ год —
  першоджерела NVIDIA не називають ліміту відео. VSS Blueprint — це
  референсна архітектура під GPU, для Termux не підходить. Gemini
  приймає публічні YouTube-посилання напряму, про NVIDIA такого не
  знайдено — потрібне завантаження й нарізка (yt-dlp/ffmpeg)
- **Висновок:** вибір між Gemini й NVIDIA потрібен лише для шару "кадри
  й звук" і його можна відкласти; текстовий аналізатор від нього не
  залежить. Коли дійдемо — один тест на коротких (до 2 хв) уривках
  відео з ШІ-птахами на обох безкоштовних рівнях
- **Статус:** огляд зроблено, рішення не прийнято

### Відео і звук для ШІ-птахів — відкладено користувачем (2026-09-20)
- **Мета користувача:** робити власні відео з «дивними птахами» зі
  звуком не гіршим, ніж у конкурентів, і перейняти їхні ключові
  прийоми. Користувач сам сказав, що поки не знає, як зробити відео й
  звук, тому тему відклав
- **Що з'ясовано:** у референсах №2 (Birds Amazing, `_ncjg2KeREI`) і
  №3 (Wild Eye Bio, `Ex2NpxfUYP0`) птахи, схоже, згенеровані ШІ. №3 сам
  заявляє це в описі; №2 — моя візуальна оцінка з двох кадрів
  [unverified]. Дикторського тексту в них немає, звук я не чув
  (transcriptor розпізнає лише мову)
- **Не досліджено:** чим створено відео й звук у конкурентів; які
  інструменти (генератор відео зі звуком, генерація звукових ефектів,
  бібліотеки записів) підходять і які в них ліцензії для монетизації.
  Жоден інструмент не названо й не перевірено
- **Перше питання після відновлення:** чим користувач збирає відео
  (ШІ-генератор, редактор із готових кліпів чи ще не починав)
- **Статус:** відкладено
- **Статус змінився (2026-09-21):** ніша птахів була тестовою, робоча
  ніша користувача — історії-оповідання. Тема звуку для ШІ-птахів для
  робочого каналу неактуальна й лишається лише довідкою

### Налаштування каналу під агента-сценариста — на паузі (2026-09-20)
- **Що зроблено:** створено `script-agent/AGENT.md` (8 частин, ще не
  закомічено). Розпочато перше налаштування, з трьох референсів
  придатний лише №1 (Before It Happened, `l8Z1TyLtdaU`, 41 хв, повний
  англомовний транскрипт). Чернетка відповідей на запитання виведена з
  нього, користувач її ще не підтвердив
- **Чекає відповіді користувача:** хто автор каналу (питання 4) і
  особливі правила каналу (питання 10); чи канал саме про птахів чи про
  рідкісних тварин загалом
- **Потрібно:** ще 1–2 референси з дикторським текстом і наявними
  субтитрами (тоді можна оформити STYLE PROFILE)
- **Статус:** на паузі
- **Статус змінився (2026-09-21):** користувач уточнив, що робоча
  ніша каналу — історії-оповідання (як референси `meErTTNhVLI`,
  `8GHfPNvBCWo`, `16zVJI94kf0`). Ніша тварин і птахів (референс
  `l8Z1TyLtdaU` та два відео зі співом птахів) була тестовою. Ці ніші не
  змішувати: чернетка відповідей вище виведена з тестового референса і
  для робочого каналу не використовується. Актуальний аналіз:
  `script-agent/analyses/stories-refs_2026-09-21.md`. Далі чекає рішення
  користувача про автора (питання 4) і особливі правила (питання 10)
- **Статус змінився (2026-09-21, кінець сесії):** перше налаштування
  каналу історій завершено. Користувач підтвердив: автор — оповідач збоку
  (третя особа), мова — англійська, формат — вигадані історії, що
  подаються як справжні, легка перевірка термінів без джерел, тривалість
  26–47 хв, тон, голос і чотири правила каналу. Готовий блок для слота
  ACTIVE PROFILE: `script-agent/profiles/stories-channel_SAVED-PROFILE.md`
  (робоча версія з позначками: `stories-channel_DRAFT.md`). Аналізатор
  (`script-agent/ANALYZER.md`, `script-agent/tools/transcript_stats.py`)
  лише готує профіль, сценарії пише окремий агент з `AGENT.md`
- **Наступні кроки:** (1) тестовий сценарій 8–10 хв: користувач дає
  тему й хронометраж, перевірити голос за профілем і відсутність
  вигаданої біографії автора; (2) доробити `ANALYZER.md` під жанр історій
  (список прогалин у кінці `analyses/stories-refs_2026-09-21.md`);
  (3) назва каналу, ширший перелік тем, підтвердження полів PACING і
  AUTHOR PERSONALITY

## Завершено (для історії)
- 2026-09-17: Локальна транскрипція (yt-dlp + ffmpeg + whisper.cpp)
- 2026-09-17: ECOSYSTEM.md — композиція агентних систем
- 2026-09-17: RULES.md + CLAUDE.md — автозавантаження правил
- 2026-09-17: Автоматизація делегування через UserPromptSubmit хук
- 2026-09-17: mcp-probe перевірка всіх 7 MCP-серверів (5 PASS, 2 вбудовані)
- 2026-09-17: Fix deepseek MCP — TMPDIR (коміт 12d4329)
- 2026-09-17: session-timer.sh — доданo EMA-прогноз тривалості кроку (α=0.3)
- 2026-09-17: transcriptor працює з ПУБЛІЧНИМИ Instagram Reels без cookies
  (метадані + кадри отримано). Раніша гіпотеза про потребу cookies — хибна
  для публічних відео.
- 2026-09-17: Виявлено вбудований Skill anthropic-skills:perevirka-dzherel
  (перевірка джерел) — Claude Code використав автоматично.
- 2026-09-18: session-close Skill (перший власний)
- 2026-09-18: Skills security audit (perevirka-dzherel,
  zvirka-bazy, skill-creator)
- 2026-09-18: SERVER.md доповнено розділом Tools (read/transcribe/status)
- 2026-09-18: HELP.txt оновлено (delegate, baton, deepseek)
- 2026-09-18: RULES.md — додано правило «повний промпт з межами,
  не покрокові інструкції, якщо задача безпечна»
- 2026-09-18: deepseek — тест на складній задачі пройдено (~112 сек,
  11/11 розділів RULES.md, деталі в TROUBLES.md)
- 2026-09-18: Дослідити вбудовані Skills Claude Code — закрито мовчки
  (звірка 2026-09-19): session-close і unlazy створено/встановлено,
  кілька Skills перевірено (perevirka-dzherel, zvirka-bazy,
  skill-creator), власні Skills можна створювати — підтверджено
- 2026-09-18: telegram_deepseek_bot.py — MVP Telegram↔DeepSeek
  оркестратор побудовано й доопрацьовано (гнучкий SYSTEM_PROMPT,
  виправлено crash на subprocess timeout). Деталі — ECOSYSTEM.md
- 2026-09-18: Unlazy Skill встановлено (project-scoped, без
  --global) — anti-laziness / acceptance gates для майбутніх
  кодових задач
- 2026-09-19: pkgtruth встановлено й зареєстровано як MCP-сервер
  (8-й підключений). Обгортка run-pkgtruth.sh (обхід бага shebang
  #!/usr/bin/env, той самий баг, що й у mcp-probe). Перевірено:
  коректно розпізнав velocity-mcp як HALLUCINATED (наш власний
  давніший 404-кейс) і реальний пакет як SAFE
  - **Відновлено 2026-09-19 (втрачено при попередньому редагуванні
    без підтвердження):** проблема, яку pkgtruth вирішує —
    галюцинації пакетів (19.7% рекомендацій LLM — вигадані, USENIX
    Security 2025, "We Have a Package for You!", Spracklen et al.,
    usenix.org/system/files/usenixsecurity25-spracklen.pdf).
    Досліджені альтернативи, які НЕ встановлено (не порівнювались
    напряму з pkgtruth, просто інші кандидати з того самого пошуку):
    slopsquash (npm v1.0.1, github.com/slopsquash/slopsquash),
    package-guard-mcp (npm v1.4.0, github.com/mlawsonking/MCP),
    package-verify-mcp (npm v0.1.0, github.com/Anicodeth/package-verify-mcp)

### ✅ memory.jsonl — ВИРІШЕНО (2026-09-17)
- **Проблема:** Claude Code не передає env-змінні в stdio-процес (баг #22571)
- **Наслідок:** `memory.jsonl` створюється в папці пакета, а не в проєкті
- **Рішення:** скрипт-обгортка, який сам ставить `MEMORY_FILE_PATH` і запускає сервер
- **Рішення:** скрипт run-memory.sh (export MEMORY_FILE_PATH + exec node)
- **Статус:** ✅ вирішено через скрипт-обгортку

### ✅ Delegate MCP — ВИРІШЕНО (2026-09-17)
- **Що:** делегування важких задач на DeepSeek через claude-code-deepseek-delegator
- **Як:** скрипт run-delegate.sh + читання ключа з agent.py (обхід бага #22571)
- **Перевірено:** файл 500 рядків → делеговано → економія 98% vs Opus
- **Hook:** файли > 300 рядків, skills > 200 рядків коду → питає «Delegate? (y/n)»
- **Статус:** ✅ вирішено

### ✅ deepseek MCP — ВИРІШЕНО (2026-09-17)
- **Що:** під-сесія Claude Code на DeepSeek для розмов/рутинних задач
- **Як:** скрипт run-deepseek.sh, читання ключа з agent.py
- **Статус:** ✅ вирішено

### ❌ deep-claude — ВІДКИНУТО (2026-09-17)
- **Що:** розглядали як альтернативу для роботи з DeepSeek
- **Причина:** MCP-сервери не працюють через DeepSeek (несумісність з
  Anthropic-сумісним шаром DeepSeek)
- **Статус:** ❌ відкинуто

### 🟡 cc_bridge — резерв
- **Мета:** міст між Claude Code і DeepSeek
- **Застереження:** потребує Go + компіляції
- **Статус:** 🟡 відкладено як резервний варіант

## Unverified Claims

### chuzom-router = llm-routing [unverified]
- На npm — 404 Not Found (перевірено 2026-09-18)
- Раніше стверджувалось, що chuzom-router — той самий проєкт,
  що llm-routing (перебрендований) — джерело невідоме, не підтверджено
- Не використовувати без верифікації

### llm-cost-router-mcp [unverified] — версія 0.3.0, немає GitHub, freemium-модель. Не встановлювати без репо.
- package.json: поле "repository" порожнє — GitHub-репо немає
  (перевірено 2026-09-18, npm registry API)
- Правило RULES.md "перевірити авторство" НЕ виконано — немає
  публічного репо, щоб перевірити офіційність/авторство
- README (з npm-дистрибутиву) згадує freemium-модель — Pro-версія
  заявлена як "coming" (додаткові платні можливості в майбутньому)
- 6 tools заявлено (estimate_cost, recommend_model, should_offload_local,
  compare_models, estimate_monthly_cost, list_models), реальна
  корисність незалежно не підтверджена
- Статус: чекає верифікації (репо/авторства) перед встановленням

## Аналізатор винесено в reference-analyzer/ (2026-09-21, не закомічено)

- Статус змінився щодо записів вище про `script-agent/ANALYZER.md`,
  `script-agent/tools/transcript_stats.py`, `script-agent/analyses/…` і
  `script-agent/profiles/…`: усе це переїхало через `git mv` у
  `reference-analyzer/` (ANALYZER.md, tools/) і
  `reference-analyzer/niches/stories/` (analysis/, profile/).
  `script-agent/AGENT.md` лишився на місці й не змінювався.
- Причина: користувач вимагає, щоб аналізатор був незалежним від проєкту
  сценариста й для кожної ніші створював власну папку (sources, analysis,
  profile, compare). Правила додано в ANALYZER.md, частина 10.
- Ще не зроблено: допрацювання ANALYZER.md під фінальні відповіді на 11
  питань (замість статусу «потребує рішення»), місце в тексті для доказів,
  виміри жанру історій. Чекають затвердження користувача після перевірки.

## Відкладено: тестовий сценарій «Закинутий готель у Вісконсині» і постмортем (2026-09-21/22)

- Користувач: «дуже багато не зрозумілого тому поки помітити як
  відкладене тому що я хочу розібратись». Усе нижче — на паузі, доки
  користувач сам не скаже продовжувати. Нічого із цього списку не
  виконувати без прямого нового запиту.
- Незакомічені зміни в робочому дереві: `RULES.md` (правила 1–4,
  оркестрація), `TROUBLES.md` (постмортем), профіль
  `reference-analyzer/niches/stories/profile/stories-channel_SAVED-PROFILE.md`
  (6 змін: сімейна драма, оповідач збоку, 129–167 слів/хв — рішення
  користувача під час симуляції, ще не підтверджено остаточно).
- Чернетка сценарію (не в git): `script-agent/output/hotel-wisconsin_36min_2026-09-21.txt`,
  4 частини, 5 368 слів. Змістовий аудит DeepSeek знайшов 26 знахідок,
  після звірки ≈9 значущих (плутанина в часі свідка Рут Мейєр, місце
  зустрічі з юристом, мотив Віктора, хук «took his uncle's life» натякає
  на вбивство, якого в сюжеті нема).
- Заходи з постмортема (TROUBLES.md, той самий запис) чекають рішення:
  M1 (патч ≈14 місць у сценарії й хук), M2 (виправити число HOOK STYLE у
  профілі: виміряно 54–117 слів, а не 120–150), M3 (узагальнити вигадані
  географічні/юридичні деталі), P1–P7 (вимір чисел перед профілем,
  змістове читання після кожної частини, шаблон брифу, request-brief/
  unlazy, розширення хука TROUBLES на delegate, з'ясування ECONNABORTED,
  формат спілкування).
