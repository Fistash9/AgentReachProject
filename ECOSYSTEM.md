# Agent Reach — Екосистема агентів

## Мета

Цей документ описує принципи, за якими майбутні проєкти в екосистемі
Agent Reach можуть доповнювати одне одного, комбінуватися між собою
і перевірятися на сумісність. Це не інструкція для терміналу і не
опис конкретної реалізації — це концептуальна карта, яку варто
прочитати перед тим, як починати новий проєкт, щоб він природно
вписався в екосистему, а не став ізольованим острівцем.

## Три рівні сумісності

Коли два агентні компоненти мають "працювати разом", сумісність
перевіряється на трьох незалежних рівнях:

1. **Протокол** — чи говорять компоненти однією версією MCP.
   Клієнт і сервер узгоджують версію протоколу під час
   `initialize`-хендшейку; несумісні версії означають, що
   компоненти не зрозуміють одне одного, навіть якщо API виглядає
   однаково.
2. **Композиція** — як кілька MCP-серверів об'єднуються в одного
   агента: через префікси інструментів, простори імен, проксі-шар.
3. **Виявлення** — як агент дізнається, що потрібний йому інший
   агент чи сервер взагалі існує і де він живе (реєстр, маніфест,
   DNS-подібний механізм).

Ці рівні незалежні: сумісний протокол не гарантує безконфліктну
композицію, а успішна композиція не означає, що агент може бути
знайдений іншими автоматично.

## MCP і A2A — два різні протоколи

- **MCP (Model Context Protocol)** — зв'язок "агент → інструмент",
  вертикальний. Агент викликає інструмент і отримує структурований
  результат. Це те, як агент отримує *можливості*.
- **A2A (Agent-to-Agent)** — зв'язок "агент → агент", горизонтальний.
  Один агент делегує задачу або веде переговори з іншим агентом,
  який має власну автономію та стан. Це те, як агенти *співпрацюють*.

Серйозні багатоагентні системи використовують обидва протоколи
одночасно: MCP — щоб дати кожному агенту доступ до інструментів,
A2A — щоб агенти могли координуватися між собою. Плутати їх —
типова помилка: намагання розв'язати задачу делегування через
інструментальний виклик (MCP) призводить до крихких, монолітних
рішень.

## Композиція через FastMCP

FastMCP пропонує два принципово різні способи об'єднати кілька
MCP-серверів в одного:

- **`import_server`** — статична копія. Інструменти дочірнього
  сервера копіюються в батьківський (з префіксом) один раз, під
  час імпорту. Якщо дочірній сервер згодом змінить свій набір
  інструментів, батьківський цього не побачить, доки імпорт не
  повториться.
- **`mount`** — динамічне живе посилання. Батьківський сервер
  проксіює виклики до дочірнього в реальному часі, тож зміни на
  дочірньому сервері одразу відображаються на батьківському.

В обох випадках критично використовувати **простори імен**
(префікси на кшталт `agent_reach:read`), щоб інструменти з різних
серверів не конфліктували за іменами.

## Реєстри для виявлення

Щоб агент міг знайти сервер, а не лише підключитися до вже
відомого, існують реєстри:

- **Офіційний MCP Registry (Anthropic)** — централізований каталог
  публічних MCP-серверів.
- **Glama, Smithery, AWS Agent Registry** — незалежні та хмарні
  каталоги з власними правилами лістингу та пошуку.
- **Маніфест `server.json`** — стандартизований опис сервера
  (ім'я, версія, можливості, залежності), який реєстри читають для
  індексації та який клієнти читають для перевірки сумісності
  перед підключенням.

## Чекліст сумісності нового агента

Перед тим як додавати новий агентний компонент в екосистему,
перевірити:

- [ ] Унікальний простір імен для інструментів (наприклад,
      `agent_reach:read`, а не голе `read`)
- [ ] Структурований I/O — вхід і вихід описані JSON Schema, а не
      довільним текстом
- [ ] Маніфест з версією та явно перерахованими залежностями
- [ ] Сумісна версія MCP-протоколу з рештою екосистеми

## Поточний стан Agent Reach

- `my_mcp_server.py` — власний MCP-сервер (агент → інструменти),
  надає інструменти `read`, `transcribe`, `status`
- Підключений до: рідного агента (DeepSeek) і Claude Code
- `transcriptor` — хостований MCP-сервер для транскрипції відео
  (YouTube, X/Twitter, Instagram, TikTok, Twitch, Vimeo та інші)
- Локальна транскрипція — резервний шлях через yt-dlp + ffmpeg +
  whisper.cpp, коли хостований сервіс недоступний або субтитрів
  немає

## Що НЕ робити

- **Не використовувати монолітного агента.** Один агент, що
  намагається сам робити все — читання, транскрипцію, статус,
  пошук — не масштабується і не композирується з іншими системами.
- **Не будувати кастомні інтеграції під кожну пару компонентів.**
  Точкова інтеграція "агент X знає про агент Y напряму" множить
  кількість зв'язків квадратично із кожним новим компонентом.
  Натомість — спільний протокол і реєстр.
- **Не плутати MCP і A2A.** Інструмент — не агент, і делегування
  задачі іншому агенту — не виклик інструмента. Використання
  неправильного протоколу для задачі призводить до крихкої
  архітектури.

## Концепція: DeepSeek-оркестратор + Claude Code через Telegram

Ідея (2026-09-18, від користувача): не окремий Telegram-бот "для Claude
Code" (як згадувалось раніше — відкладено), а обгортка навколо чату з
DeepSeek, яка сама делегує задачі Claude Code через A2A-подібний шар
(див. розділ "MCP і A2A" вище) — це і є той самий "агент вищого
порядку", про який каже CONTEXT.md.

Підтверджено (2026-09-18, WebSearch): A2A (Agent2Agent) — реальний
відкритий протокол Google, переданий Linux Foundation, прийнятий
150+ організаціями станом на квітень 2026 — саме для делегування
задач між агентами. LangGraph checkpointing — реальний паттерн для
збереження стану на кожному кроці з можливістю відновлення після
збою і "human-in-the-loop" паузами — релевантний для пункту про
керування контекстом нижче.

### З чого складається
1. **Telegram як інтерфейс** — спілкування з DeepSeek у звичному чаті,
   без термінала.
2. **DeepSeek-чат як оркестратор** — веде розмову з користувачем,
   формулює промти для Claude Code, отримує результат назад.
3. **Claude Code як виконавець** — вирішує технічні задачі (аналогічно
   тому, що DeepSeek MCP уже робить зараз як під-сесія — див. RULES.md
   "DeepSeek — вибір інструменту").
4. **Керування контекстом** — обгортка стежить за заповненням контексту
   DeepSeek-чату і сама переводить у новий чат, коли потрібно, без
   втрати важливого (той самий принцип, що вже реалізований у цій
   сесії через checkpoint CONTEXT.md + baton_pass, тільки для іншого
   чату).
5. **Постійний цикл рефлексії під час задачі** — не тільки виконати,
   а й проаналізувати: чи покращення можливе, чи була схожа помилка
   раніше (перевірка на кшталт TROUBLES.md, тільки автоматична, а не
   ручний grep).
6. **Нагадування** — обгортка сама підказує користувачу важливе, коли
   настає момент (аналог baton_pass, але ініціативний, не за запитом).

### Зв'язок з наявним
- Telegram-бот для Claude Code вже згадувався в CONTEXT.md
  ("Відкриті питання") — ця концепція його замінює конкретнішим
  баченням.
- A2A-шар делегування вже є в BACKLOG.md ("Дослідити") — саме цей
  протокол, а не MCP, потрібен для зв'язку DeepSeek↔Claude Code.
- TROUBLES.md вже виконує роль "чи ми вже з цим стикались" — ідея
  в тому, щоб це стало автоматичною перевіркою, а не ручним кроком.

### Ризики — що може піти не так через рік (2026-09-18)
1. **Автоматичне "чи ми вже це вирішували" (п.5) — небезпечна
   спрощеність.** Семантичне зіставлення нової помилки зі старими
   може дати хибний збіг: система вирішить "уже бачили" для схожої,
   але насправді іншої проблеми, і пропустить реальне розслідування.
2. **DeepSeek як оркестратор без перевірки власних інструкцій.**
   DeepSeek уже падав цієї сесії (permission, timeout). Немає шару,
   що перевіряє промт від DeepSeek до Claude Code перед виконанням —
   хибна інструкція піде далі без контролю.
3. **Автоматична ротація контексту може тихо губити важливе.**
   Той самий ризик, що ми виявили з ручним baton_pass цієї сесії
   (агент вирішує, що важливо, без показу diff), тільки повністю
   автоматизований — без людини, яка побачить різницю.
4. **Секрети — новий вектор.** Telegram-бот потребує власного токена
   (ще один ключ поруч з agent.py і DeepSeek API key) — ще одна точка
   витоку.
5. **Немає kill switch.** Якщо оркестратор зациклиться або дасть
   Claude Code погану інструкцію — немає опису, як це перервати
   негайно.
6. **Дрейф версій за рік.** pip `mcp` пройшов з 1.30 до 2.2.0 за
   кілька тижнів цієї сесії. DeepSeek API, MCP-протокол, Claude Code —
   усе змінюється; концепція не описує перевірку сумісності з часом.
7. **"Нагадування" (п.6) без конкретного тригера — не буде
   реалізовано.** "Коли настає момент" не визначає сигнал (час?
   подія? ключове слово?) — розмиті пункти без тригера традиційно
   не доходять до реалізації.

### Дебати між агентами (п.5) — критерії та ефективність (2026-09-18)

Перевірено власним тестовим дебатом (2 агенти, 2 раунди) + дослідженнями
в інтернеті. Ключова знахідка: **дебати не завжди ефективніші за просте
голосування**.

**Порівняння підходів:**

| Підхід | Що це | Точність vs один агент | Вартість |
|---|---|---|---|
| Один агент | Питаєш один раз | база | найдешевше |
| Голосування (Majority Vote) | Питаєш 2-3 рази НЕЗАЛЕЖНО, береш найчастішу відповідь | часто майже так само добре, як дебати | дешевше за дебати |
| Повний дебат | Агенти сперечаються, бачать відповіді одне одного | +5-40% залежно від задачі | найдорожче (найбільше токенів/часу) |

Дослідження "Debate or Vote" (2508.17536): виграш дебатів здебільшого
йде не від "суперечки" самої по собі, а від опитування кількома
"головами" — це можна отримати дешевше просто голосуванням.

**Критерії якісного дебату (якщо колись реалізовувати повноцінно):**
1. Незалежна відповідь перед взаємодією (Раунд 0 — агенти НЕ бачать
   один одного, щоб уникнути упередження зі старту)
2. Адаптивна кількість раундів, не фіксована — приріст точності росте
   до ~3-4 раунду, далі плато; "хибний консенсус" (обидва помиляються,
   але узгоджено) найнижчий на раунді 2 (3.9%), знову росте до раунду 5
   (5.1%) — більше раундів не завжди краще
3. Окремий суддя для консенсусу, не самі дебатери
4. Застереження: переконливий, але хибний аргумент може перемогти
   правильний ("vivid but incorrect" ефект) — точність може навіть
   ПАДАТИ під тиском узгодження (сикофантія)

**Практичний висновок для п.5 ("постійний цикл рефлексії"):** замість
повноцінних дебатів — простіше й дешевше опитати DeepSeek те саме
питання 2-3 рази незалежно і звірити відповіді (голосування), а не
будувати дорогий механізм дебатів, який не гарантовано ефективніший.

### Статус
Концепція, не план реалізації. Технічні деталі (як саме автоматизувати
кожен з 6 пунктів, і як закрити 7 ризиків вище) — дописувати сюди в
міру появи ідей.

## Концепція: перевірка правильності розуміння запиту (2026-09-20)

**Мета користувача:** щоб запит простими словами давав результат не
гірший за якісний промпт з деталями, а не "на око". Стосується і
людина→агент, і агент→агент (передача завдання між агентами).

Пошук в інтернеті (WebSearch, US-only) + звірка цифр з абстрактами
первинних статей. Позначки: **Sourced** — цитату перевірено в
абстракті/тексті; **[unverified]** — бачив лише вторинне джерело
або переказ пошуку.

### Які механізми існують

| Механізм | Що це | Результат |
|---|---|---|
| Уточнення до виконання | Модель знаходить неоднозначність і ставить цільові питання | ClarifyGPT (2310.10996): Pass@1 GPT-4 70.96%→80.80% на MBPP-sanitized; середнє по 4 бенчмарках 68.02%→75.75% (GPT-4), 58.55%→67.22% (ChatGPT). CLARITI (2604.14624, 8B): дорівнює GPT-5 за розв'язаними недовизначеними задачами з на 41% меншою кількістю питань. Sourced |
| Окремий крок переписування запиту | Одна модель переписує питання за контекстом, інша відповідає | Humanity's Last Exam (2603.04454): 0.14→0.37 (gpt-5-mini). Автори: самим промптингом на етапі відповіді не відтворюється, потрібні окремі фази. Sourced |
| Перевірка результату проти запиту | Verifier-агент звіряє результат з початковою інструкцією, за потреби — перепланування | BMW Agents (2406.20041) описує механізм; цифр успішності не знайдено. [unverified] |

### Застереження зі знайдених досліджень (Sourced)
- **Моделі бачать неоднозначність, але рідко питають** (2605.25284):
  на пряме питання про неоднозначність розпізнають, а в звичайному QA
  "переважно за замовчуванням відповідають напряму"; підключений
  контекст ще зменшує ймовірність уточнень. Наш висновок (інференція,
  не з статті): механізм має бути зовнішнім (хук/skill), а не
  проханням "будь уважнішим".
- **Переписування може шкодити** (2603.13301): nDCG@10 −9.0% (FiQA),
  +5.1% (TREC-COVID), без ефекту на SciFact. Важить напрямок заміни
  термінів, а не сама заміна.
- **Просто додати всі вимоги не допомагає** (2505.13360): недовизначені
  промпти вдвічі частіше "регресують" при зміні моделі чи промпту, іноді
  з падінням точності понад 20%; requirements-aware оптимізація дає
  +4.8% у середньому.
- **Між агентами** (MAST, 2503.13657, HTML v3): "disobey task
  specification" (FM-1.1) — 11.8% з 1642 трас. Категорійні відсотки
  41.8 / 36.9 / 21.3 з блогів у самій статті не знайдено —
  [unverified].

### Чого не знайдено
Дослідження, яке прямо порівнює "простий запит + механізм уточнення" з
"якісним детальним промптом, написаним людиною". Найближче — ClarifyGPT
(уточнення проти двозначного запиту, не проти повної специфікації).
ClarifyCodeBench (2607.00711) ймовірно ближчий, але я бачив лише
переказ пошуку — [unverified].

### Статус
Знахідки записано. Рішення, чи й як впроваджувати механізм у
Agent Reach, — не прийнято.

## Концепція: Hermes Agent і "агентні офіси" — порівняння (2026-09-20)

Контекст: питання "агент як ще одні руки" + терміни "Hermes agents" і
"агентні офіси", про які користувач колись чув в інтернеті без
конкретного джерела. Позначки: **Sourced** — джерело прочитано;
**[unverified]** — лише переказ пошуку або вторинне джерело. Сторінки
GitHub/docs читались через переказ WebFetch (мала модель); файли ECC —
напряму з локального клону.

### Hermes Agent (Nous Research)
Повноцінний агентний фреймворк, не лише модель (початкова здогадка
"моделі Hermes" була неповною).
- MIT. "Self-improving": агент сам створює й покращує скіли, веде
  пам'ять із періодичними підказками, шукає по минулих сесіях (FTS5).
  Sourced
- Один шлюз для Telegram, Discord, Slack, WhatsApp, Signal і CLI;
  вимагає Python 3.11 і Node; **Termux підтримується** (ручна установка
  з `.[termux]`). Sourced
- Делегування в Claude Code (docs Hermes): `claude -p` (print-режим),
  інтерактивний режим через tmux; керує `--allowedTools`,
  `--permission-mode`, `--max-turns`, `--output-format json`.
  Попередження в docs: не надсилати "сліпий Enter" на запити дозволів,
  контекст деградує після ~70%. **Критерії завершення явно не
  описані.** Sourced
- 247k зірок — цифра з переказу, [unverified]

### Зв'язок Hermes ↔ ECC (Sourced, `docs/HERMES-SETUP.md` у клоні)
"Hermes is the operator shell. ECC is the reusable system behind it."
Схема: Telegram/CLI → Hermes → скіли+хуки+MCP+Memory Vault ECC → зовнішні
сервіси. Reel показував лише нижній шар (ECC). README ECC називає hermes
серед "experimental/minimal adapters", шлях установки:
`./install.sh --profile minimal --target hermes`.

### "Агентні офіси"
Єдиного стандарту немає — назва для кількох проєктів. Sourced:
AgentOffice (harishkotra/agent-office, MIT) — піксель-арт офіс, агенти з
ролями (Engineer, PM, Designer) самі ставлять одне одному задачі й
наймають нових; працює на Ollama (llama3.2 за замовчуванням), Node >=18.
TaskVille, agent-virtual-office (KbWen), Agent Heights — лише з
результатів пошуку, [unverified]. Суть однакова: ролі, дошка задач,
передача робіт, видимий статус; піксель-арт — візуалізація поверх
звичайної оркестрації.

### Зіставлення з Agent Reach
- `telegram_deepseek_bot.py` (див. вище в цьому файлі) — це вже мала
  версія тієї ж схеми: Telegram → оркестратор → `claude -p` з режимом
  `plan` за замовчуванням. Hermes робить те саме зрілим способом
- "Руки" (виконавець) уже існують. Бракує специфікації, критерію
  завершення й перевірки результату проти запиту — це ризик №2 вище
  (немає перевірки, чи інструкція DeepSeek коректна) і
  дослідження вище (MAST: "disobey task specification" 11.8% трас).
  Hermes docs цю перевірку теж перекладають на оператора
- Автоматичне створення скілів у Hermes = самомодифікація без показу;
  суперечить принципу "спершу показати, потім змінювати"
  (confirm-before-lossy-edits) — інференція, не з джерела
- Termux: Hermes документує підтримку, ECC для Claude Code — ні. Запуском
  не перевірено

### Висновок (рекомендація, не рішення)
Нового агента не будувати. Спершу — шаблон delegation brief: мета +
умова зупинки + межі (RULES.md вже вимагає таку структуру промпту) і
автоматична перевірка результату проти цього брифу.

### Статус
Знахідки записано. Рішення про Hermes/ECC/агентів — не прийнято.

## Порівняння: скіли Matt Pocock vs наша система (2026-09-23)

Мета: вирішити, чи ставити плагін `mattpocock-skills` (v1.2.3, 25 скілів,
офіційний маркетплейс Anthropic, MIT) і чи варто переробити наші файли
під його систему. Метод: 5 пар "їхнє ↔ наше", кожну читав deepseek-v4-flash
(curl сирих SKILL.md + Read наших файлів); 14 ключових цитат звірено мною
curl+grep — усі 14 існують дослівно (дві — у SKILL-MECHANICS.md/SKILL.md
того ж скіла).

Загальне: у плагіні НЕМАЄ хуків. 14 скілів ручні (`disable-model-invocation:
true`, лише `/назва`), 11 — автоматичні. Кожен автоматичний скіл тримає свій
опис у контексті ЗАВЖДИ ("permanent context load", writing-for-agents) —
тобто весь плагін = +11 постійних описів у кожній сесії.

### 1. CONTEXT.md ↔ domain-modeling — РІЗНІ задачі
- Їхній CONTEXT.md: "It is a glossary and nothing else" — лише терміни
  домену, з `_Avoid_` для синонімів; оновлюється одразу ("Don't batch
  these up"); рішення — в ADR лише якщо "Hard to reverse" / несподіване /
  справжній компроміс.
- Наш CONTEXT.md: паспорт + журнал стану, "Оновлюється ДО стиснення
  контексту", датовані знімки.
- Конфлікт: скіл автоматичний і спрацює на правці нашого CONTEXT.md,
  тягнучи його в глосарій. Рекомендація: лишити наше; можливо взяти
  блок "Мова проєкту" з `_Avoid_` і 3 критерії ADR як фільтр для записів.

### 2. RULES.md ↔ writing-for-agents — у них глибша теорія
- Їхні важелі: context load (що завжди в контексті — коштує щоразу),
  точне формулювання покажчика ("The pointer's wording, not its target"),
  критерій завершення кожного кроку, "steering by prohibition drags the
  forbidden behaviour into context".
- Наш RULES.md: ~200 рядків завжди в контексті, багато "Заборонено"/"Не…",
  пояснення-історії всередині правил, дубль переліку файлів (Навігація і
  Запис знань). Зате є те, чого в них нема: секрети, журнал vs знімок,
  рівні знань.
- Рекомендація: адаптувати RULES.md (не замінювати): покажчики з умовою
  "коли читати"; пояснення-історії винести в TROUBLES.md; частину
  заборон переформулювати позитивно. Будь-яка з цих правок СКОРОЧУЄ
  текст — лише після показу diff.

### 3. request-brief ↔ grilling — схожі, різні об'єкти
- grilling: допит про ідею/рішення, питання раундами ("Ask the whole
  frontier in one round") з рекомендованою відповіддю до кожного; факти
  шукає сам ("Finding facts is your job, never the user's"); результату
  не перевіряє.
- request-brief: бриф на роботу, максимум 1 питання, є "Готово, коли" і
  звірка результату.
- Конфлікт: grilling автоматичний → на один запит можливі два "допити".
- Рекомендація: лишити request-brief; взяти ідею "рекомендована відповідь
  до питання"; grill-me (ручний) — для сирих ідей, окремо від брифу.

### 4. Рівні знань ↔ research — у нас строгіше
- research: лише первинні джерела ("primary sources"), кожне твердження
  до джерела-власника, результат — окремий .md у репо, фоновий агент
  ("background agent").
- Наше: Sourced / [unverified] / Hallucinated, перевірка командою
  (npm view, curl -I), "той самий пошук перед 'не варто'".
- Конфлікт: research автоматичний і плодить нові .md поза TROUBLES/BACKLOG.
- Рекомендація: лишити наше; взяти "до джерела-власника, не до переказу".

### 5. session-close/baton ↔ handoff — різні філософії
- handoff (ручний): вільний підсумок у temp ОС (поза репо), "Redact any
  sensitive information", не дублювати — посилатись шляхом, секція
  "suggested skills".
- Наше: структуровані поля baton, git-перевірки, журнали в проєкті.
- Рекомендація: лишити наше; додати в session-close крок "перевірити, що
  в baton_pass нема ключів" і "посилатись на файли, не дублювати".

### Висновок
Замінювати наші файли не варто: в 4 з 5 пар задачі різні або наше строгіше.
Найцінніше в них — теорія writing-for-agents (для RULES.md) і кілька дрібних
ідей. Весь плагін = 4 автоматичні скіли, що зачіпають наші ключові файли
(domain-modeling, writing-for-agents, research, grilling) + постійні описи в
контексті. Безпечніше взяти вибірково (`npx skills`): grill-me + grilling,
wait-what, diagnosing-bugs, teach — [чи можна вимкнути окремі скіли плагіна,
не перевірено].
Статус змінився (2026-09-23): за докою — скіли плагіна мають простір
імен (конфлікту з вбудованим `/code-review` немає); плагін можна ставити
з `--scope local` (лише цей репо); окремий скіл плагіна вимикається
лише разом з усім плагіном, `skillOverrides` для скілів плагіна — не
підтверджено (TROUBLES.md «Перевірка здогадок сесії 2026-09-23»).

### Статус
Звіт записано. Жодних змін у RULES.md / CONTEXT.md / скілах не зроблено —
кожна окремо, після показу diff і "так" користувача.
